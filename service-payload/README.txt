{{APP_NAME}}
================

This USB stick was prepared with {{APP_NAME}} ({{ORG_NAME}}).

Mode: {{MODE_TITLE}} ({{MODE_ID}})
Instance: {{INSTANCE_ID}}
Created: {{CREATED_AT}}
Tool version: {{APP_VERSION}} ({{APP_BUILD}})

What you can do
---------------
- Live ISO mode / Both: copy .iso files into the "isos" folder on this volume,
  then boot the stick and pick an entry from the boot menu.
- Installed mode / Both: full Linux installs live on a separate Btrfs pool
  (not visible in Windows File Explorer). Use {{APP_NAME}} to add, remove,
  snapshot, or reconfigure installs.

Safety
------
Formatting or reconfiguring can erase data. Always confirm the target disk
in the app before proceeding.

More info
---------
Homepage: {{HOMEPAGE_URL}}
Docs:     {{DOCS_URL}}
Issues:   {{ISSUES_URL}}
